import React from 'react';
import { Button } from './ui/button';

const LoginButton = () => {
  // need to implement clean logic
  return (
    <div className="">
      <Button className=' rounded-full'>Connect</Button>
      {/* <appkit-button size="sm" balance="hide" />{' '} */}
    </div>
  );
};

export default LoginButton;
